import { Tooltip } from "@chakra-ui/react";

export function EditStat({ plant }) {
  return (
    <>
      <Tooltip
        label="Add Species"
        aria-label="A tooltip"
        bg="main.900"
        color="back.900"
      >
        Undefined
      </Tooltip>
    </>
  );
}
